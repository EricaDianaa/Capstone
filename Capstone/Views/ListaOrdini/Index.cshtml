@model IEnumerable<Capstone.Models.ListaOrdini>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Eventi.NomeEvento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ordini.IdOrdini)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantità)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Eventi.NomeEvento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ordini.IdOrdini)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantità)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdListaOrdine }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdListaOrdine }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdListaOrdine })
        </td>
    </tr>
}

</table>
