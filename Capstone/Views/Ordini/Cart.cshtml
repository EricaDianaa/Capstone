@using Capstone.Models 
@{
    ViewBag.Title = "Cart";
}

<link href="~/Content/StyleCart.css" rel="stylesheet" />
<div id="Container" class=" p-3">

    @if (Session["Carello"] != null)
    {<div class=" text-primary text-center ">
            <h2 id="Title">Il tuo carrello</h2>
        </div>
        foreach (EventiOrdini e in Model)
        {
            <div  class="card  mb-3  mx-auto" id="Card">
                <div class="row g-0">
                    <div class="col-md-6">
                        <img src="~/Content/Img/@e.FotoCopertina" class="img-fluid d-flex h-75 w-75 mt-3 justify-content-center rounded-5 mx-auto my-auto" alt="...">
                    </div>
                    <div class="col-md-6">
                        <div class="card-body">
                            <h5 class="card-title "><a class="text-warning text-decoration-none" href="/Eventi/Details/@e.IdEvento"> @e.NomeEvento.ToUpper()</a></h5>
                            <p class="card-text"><strong>Prezzo: </strong> @string.Format("{0:C}", @e.Prezzo)</p>
                            @if (e.DataDa != null && e.DataEvento != null)
                            {
                                <p class="card-text"><strong>Data inizio evento: </strong>   @string.Format("{0:d}", @e.DataDa)</p>
                                <p class="card-text"><strong>Data fine evento: </strong>   @string.Format("{0:d}", @e.DataEvento)</p>
                            }
                            else if (e.DataDa == null && e.DataEvento == null)
                            {

                            }
                            else
                            {
                                <p class="card-text"><strong>Data fine evento: </strong>   @string.Format("{0:d}", @e.DataEvento)</p>
                            }
                            <p class="card-text"><strong>Indirizzo: </strong>  @e.Indirizzo</p>
                            <p class="card-text">  <strong>Luogo : </strong> @e.Luogo  </p>
                            <p class="card-text">  <strong>Quantità : </strong> @e.Quantità  </p>

                            <a href="/Ordini/CartRemove/?Id=@e.IdEvento" class="btn btn-danger" id="Elimina">-</a>
                            <a href="/Ordini/AddQuantità/?Id=@e.IdEvento" class="btn btn-danger" id="Elimina">+</a>
                            <a href="/Ordini/CartRemoveAllProdotto/?Id=@e.IdEvento" class="btn btn-danger" id="EliminaP">Elimina</a>
                        </div>
                    </div>
                </div>
            </div>

            @*<div class="card bg-black text-white mb-3">
                    <div class="row g-0">
                        <div class="col-md-4">
                            <img src="~/Content/Img/@p.FotoCopertina" class="img-fluid rounded-start " alt="...">
                        </div>
                        <div class="col-md-8">
                            <div class="card-body">
                                <h5 class="card-title"> @p.NomeEvento</h5>
                                <p class="card-text"><strong>Prezzo: </strong> @string.Format("{0:C}", @p.Prezzo)</p>
                                <p class="card-text"><strong>Data evento: </strong>   @string.Format("{0:d}", @p.DataEvento)</p>
                                <p class="card-text"><strong>Indirizzo: </strong>  @p.Indirizzo</p>
                                <p class="card-text">  <strong>Luogo : </strong> @p.Luogo  </p>
                                <p class="card-text">  <strong>Quantità : </strong> @p.Quantità  </p>

                                <a href="/Ordini/CartRemove/?Id=@p.IdEvento" class="btn btn-danger" id="Elimina">-</a>
                                <a href="/Ordini/AddQuantità/?Id=@p.IdEvento" class="btn btn-danger" id="Elimina">+</a>
                                <a href="/Ordini/CartRemoveAllProdotto/?Id=@p.IdEvento" class="btn btn-danger" id="EliminaP">Elimina</a>
                            </div>
                        </div>
                    </div>
                </div>*@

        }
        <div id="Total" class=" border border-white text-center p-2">
            <h3 class=" ">Totale carrello: @string.Format("{0:C}", @ViewBag.totale) </h3>
            <a href="/Ordini/Create" class="btn btn-warning">Concludi l'ordine</a>
            <a href="/Ordini/CartRemoveAll" class="btn btn-warning">Svuota il carello</a>
        </div>
    }
    @if (Session["Carello"] == null)
    {
        <div class="bg-dark text-primary text-center border border-white border-2">
            <h2>Il tuo carrello è vuoto</h2>
        </div>
    }



</div>


@*@section scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            $("#Elimina").click(function () {
                var valore = $(this).data("Id")

        console.log("Val",valore)
            })
        })

    </script>
}*@